1. 변수로 지정한 특정 경로를 기준으로 한다.
2. 제외할 파일 이름(exclude_files), 폴더 이름(exclude_folders), 확장자(exclude_extensions)를 지정하고 exclude라는 하나의 변수로 묶는다.
3. 지정한 경로를 기준으로 exclude를 제외하고 하위 디렉토리에서 파일의 생성 시각(Date_of_creation), 수정 시각(Modified_times)을 저장한다
4. 수정 시각 중 가장 마지막 수정 시각은 마지막 수정 시각(Last_modified)으로 저장한다.

?.  중첩된 구조로 JSON 파일로 저장한다.

1. 기존 데이터를 가져오고
2. 새로운 데이터도 가져온 후
3. 기존 데이터와 새로운 데이터를 중첩없이 병합 후
4. 이름을 변경하기 전인 파일에 저장 후
5. 저장한 파일 이름의 타임 스템프를 최신으로 수정하여 저장한다